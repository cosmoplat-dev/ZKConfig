----------------------------------------------
通过dubbo做的一个示例
里面对zookeeper和jedis做了封装
可通过项目启动的时候对zookeeper节点进行初始化并根据配置可设置IP访问权限或者读写权限。
对zookeeper的监听方法做的封装，初始化时缓存监听需要监听的节点，修改节点数据时，通过监听机制及时刷新缓存数据。
对redis连接池做了封装，基于zookeeper的监听方法，当修改redis的参数配置或者连接地址时，可使项目的redis随即发生变化。